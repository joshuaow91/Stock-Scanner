import { ScenarioTransformPipe } from './scenario-transform.pipe';

describe('ScenarioTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new ScenarioTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
